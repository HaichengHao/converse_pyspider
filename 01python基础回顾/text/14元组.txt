tuple--元组
空元组的创建 t=tuple()
特点
    可以理解为不可变的列表，指的是内存地址不可变
    用()包起来
    可以进行索引和切片
    但不可变，不可以修改 'tuple' object does not support item assignment
    元组如果只有一个元素，那么其数据类型将会是括号内的数据的数据类型，因为它会把括号识别为数学含义
    但是！如果加上逗号，则会识别为元组